import { listenerRowState } from 'maxpanel-ember/helpers/listener-row-state';
import { module, test } from 'qunit';

module('Unit | Helper | listener row state');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = listenerRowState([42]);
  assert.ok(result);
});
