import { cellstate } from 'maxpanel-ember/helpers/cellstate';
import { module, test } from 'qunit';

module('Unit | Helper | cellstate');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cellstate([42]);
  assert.ok(result);
});
